class: Tween() {
  awake = function(target, a, vars, ease, bounce)
    self.target = target
    self.a = a

    self.initial = {}
    self.targets = {}

    self.ease = ease
    self.bounce = bounce

    for var, target in pairs(vars) do

    end
  end;
}
